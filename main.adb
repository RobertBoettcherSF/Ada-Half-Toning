-- main.adb
-- Entry point for generic execution (Compilation requirement)
with Ada.Text_IO; use Ada.Text_IO;

procedure Main is
begin
   Put_Line("Halftoning Project compiled successfully.");
   Put_Line("Run `make test` to execute the V&V test suite.");
end Main;
